<h1>Add a bookmark for <%= @list.name %> list</h1>

<%= simple_form_for([@list, @bookmark]) do |f| %>
  <%= f.association :movie %>
  <%= f.input :comment %>
  <%= f.association :list %>
  <%= f.button :submit %>
<% end %>
